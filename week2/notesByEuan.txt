 type in:
 gdb ./a.out
 b2 // if makes error
 bt // back trace

there is a way to compile with debugging info
-g should be put in the make file
use make files you nonce
put the -g flag before the file to display  code
